# models/

Trained model artifacts (`.pkl` files) are saved here by `train_model.py`. They are gitignored — regenerate them by running the pipeline (see main README) rather than expecting them to be present after a fresh clone.
